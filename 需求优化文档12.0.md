# A股量化工具 — 12.0 需求优化文档

> 本轮主题：**策略引擎**。把全站已落库的行情、资金、量化、财报、板块、宏观、商品、全球指数等，收成统一特征与关键信息，再供智能分析与智能选股消费。
>
> **本轮交付范围（先设计、后实现）**：完整设计逻辑 + 设置页二级菜单「策略引擎配置」展示蓝图与配置面 + 智能选股信息架构重排。  
> **本轮明确不做**：不跑采集任务、不写特征表数据、不编造引擎评分、不改购买指数公式、不把财报并入购买指数。

- **版本**：v12.0
- **整理日期**：2026-08-15
- **上游文档**：需求文档 1.0、产品优化文档、需求优化文档 2.0～11.0
- **编号约定**：本版新增 **FR12-xx**；缺口 **GAP12-xx**；本轮只验收设计项 **AC12-Dx**

---

## 0. 一句话目标

做一层平台级 **策略引擎**：定时采集全站本地数据 → 提取关键信息与关键指标 → 产出可追溯的分析快照 → **智能选股**（及其他下游）只消费引擎结果，不再各自临时拼数。

---

## 1. 代码核对结论（设计前必读）

### 1.1 数据已经很多，但没有「一层脑」

| 域 | 现有落库 / 服务 | 谁在用 | 问题 |
|----|-----------------|--------|------|
| 行情快照 | `stock_snapshot` | 看板、筛选、推荐、智能选股 | 各入口各自 SQL |
| 量化四指标 | `stock_metrics`（购买指数/情绪/暗盘/企稳） | 策略 A–H、智能选股权重 | 智能选股不读策略命中 |
| 财报评级 | `stock_finance_grade` A/B/C/D | 筛选/推荐/智能选股 | 独立维度，保持 |
| 板块资金 | `sector_flow_daily` + 即时聚合 | 板块资金页 | 智能选股只用「行业当日净流入」一项 |
| 买/卖策略 | `strategy.py` 方案 A–H，SQL 硬编码 | 设置→选股策略、提醒、买点窗 | 与智能选股平行，互不消费 |
| 宏观快讯/日历 | `intel_cache` / `macro_event` | 宏观情报页 | 11.0 曾预留「近 3 日快讯命中」，智能选股实际几乎未用 |
| 官方政策 | `official_policy` 近半年 | 宏观→官方政策信息 | **无任何选股消费** |
| 热度词汇 | `hot_term` 近两周 | 宏观→热度词汇 | **无任何选股消费** |
| 商品/全球 | 缓存 + 关联个股 | 各自一级菜单 | 不进入综合分 |
| 公告/股东 | `announcements` / `stock_holders` | 个股页 | 不进入综合分 |
| AI | 独立客户端 + 智能选股点评开关 | AI 分析 / 智能选股点评 | 点评吃的是选股表，不是引擎摘要 |

结论：**不是缺数据，是缺统一采集、提取、快照、消费协议。**

### 1.2 选股入口仍分裂（GAP12-1）

| 入口 | 现在做什么 | 12.0 后应扮演 |
|------|------------|----------------|
| 设置→选股策略 | 启用买/卖方案 A–H | **规则插件**，命中写入引擎「信号包」 |
| 设置→策略引擎配置 | （本版新建） | **引擎开关、源、频率、权重、消费开关** |
| 智能选股 | 8 维权重现场打分 | **引擎结果工作台**（综合/命中/催化/快照） |
| 个股筛选 | AND 条件器 | 保留，不经引擎也可查；可选「只看引擎池」 |
| 个股推荐 | 固定榜单 | 保留；榜单可标注是否已在引擎池 |
| AI 选股 | 一句话语义筛 | 保留语义；名单优先从引擎特征过滤 |

### 1.3 智能选股现状缺口（GAP12-2）

现行 `smartpick.run`：筛器条件 + 硬开关 + 8 维加权。缺：

- 不读官方政策 / 热词 / 日历 / 商品 / 全球指数
- 不读买点/卖点方案命中标签
- 没有市场体制（牛/熊/震荡）作为环境分的显式输入
- 理由句只有购买指数、财报、买比、行业净流入，解释力不够
- 页面是单卡表单，无法按「引擎产物」分栏工作

### 1.4 不可破的产品铁律（沿用）

1. 财报评级 **不** 并入购买指数。  
2. 禁止用涨跌幅伪造多日资金。  
3. 无财报评级不得显示为 A。  
4. 策略 SQL **只** 在服务端硬编码。  
5. LLM 失败回退本地规则，不得假装成功。  
6. 引擎缺数时展示原因，**禁止编造** 政策文件、热度、股东人数、资金。

---

## 2. 策略引擎总体设计（FR12-01）

### 2.1 定位

策略引擎是 **服务层中间件**，不是又一个选股模板。

```
各数据源/定时任务 ──写入──► 本地库（已有表）
                              │
                    ① 采集器 Collector（只读本地库+只读缓存）
                              │
                    ② 提取器 Extractor（关键信息 / 关键指标）
                              │
                    ③ 分析器 Analyzer（体制、轮动、冲击、向量、信号）
                              │
                    ④ 快照仓 Snapshot Store
                              │
          ┌─────────┬─────────┼─────────┬─────────┐
          ▼         ▼         ▼         ▼         ▼
     智能选股   智能提醒   看板摘要   AI 点评   日后回测
```

引擎 **不** 直接打行情 HTTP（沿用现有调度与数据源健康）。它消费已经同步好的本地事实。

### 2.2 四层职责

| 层 | 职责 | 失败策略 |
|----|------|----------|
| 采集 | 按域列出「本拍有哪些表、覆盖到哪一天、条数、新鲜度」 | 某域失败则该域 `available=false`，其它域继续 |
| 提取 | 把宽表压成关键字段与短文本事实 | 字段缺失记 `null` + `missing_reason`，不填默认「好」 |
| 分析 | 规则计算（主）+ 可选 LLM 总结（辅） | LLM 关闭或失败时只用规则摘要 |
| 快照 | 按 `asof`（北京交易日）落一份可回放结果 | 只追加/替换当日，不改历史日（便于以后回测） |

### 2.3 产出对象（下游只认这些）

| 对象 | 粒度 | 用途 |
|------|------|------|
| `MarketRegime` | 全市场 1 条 | 体制：偏多/震荡/偏空 + 温度 + 量能 + 一句环境 |
| `SectorRotation` | 行业/概念 TopN | 流入/流出、热度、与热词/政策的关联 |
| `MacroCatalyst` | 事件/政策/热词 | 近窗口关键信息，带方向与波及板块 |
| `StockVector` | 每只股票 1 行 | 标准化 0–100 维 + 缺失标记 + 命中的策略/催化 ID |
| `SignalBundle` | 买点池 / 卖点池 | 来自方案 A–H，带 `plan_id`、方向 |
| `EngineBrief` | 1 份 | 给人看的关键信息清单（供 UI 与 AI 语境） |

智能选股 **禁止** 再直接 `SELECT` 拼一套与引擎不同口径的综合分；综合分 = `f(StockVector, 用户权重, 硬条件)`。

过渡期（12.0.1 实现阶段）：若当日引擎快照不存在，智能选股「综合选股」仍可走现行 `smartpick.run`，但必须在页头写「未接入引擎快照，使用现场计算」。本轮占位页已按此文案预留。

---

## 3. 全站数据域与关键指标目录（FR12-02）

采集清单必须可配置（设置→策略引擎配置里的源开关）。默认 **全开**；关掉的域不进入向量，对应维度分记缺失。

### 3.1 市场与个股

| 域 ID | 来源 | 提取的关键指标 | 关键信息（短文本） |
|-------|------|----------------|-------------------|
| `market` | 指数快照、涨跌家数、成交额 | 上证/深成/创业涨跌、上涨家数比、两市成交额 | 「放量上涨/缩量下跌」一句 |
| `quote` | `stock_snapshot` | 价、涨跌、5/20 日涨跌、量比、换手、振幅、市值、PE/PB | — |
| `flow` | 快照资金字段 | 主力净流入、5 日净流入、主力买比、散户买卖（估算） | 吸筹/派发一句 |
| `metrics` | `stock_metrics` | 购买指数、情绪、暗盘、企稳、RSI、MACD、均线、pos60 | 分档标签（极佳/较好/…） |
| `finance` | `stock_finance_grade` | A/B/C/D、分数、摘要 | 无评级=`—`，维度分 50 不奖不罚 |
| `holders` | `stock_holders` | 有则取户数变化等已规范化字段 | API 失败则整域缺失，不编 |

### 3.2 板块与推荐

| 域 ID | 来源 | 关键指标 | 关键信息 |
|-------|------|----------|----------|
| `industry` | 行业聚合 / `sector_flow_daily` | 涨跌、净流入、多日净流入（有账本才有） | 领涨/领跌行业名 |
| `concept` | `concept_board` + `concept_map` | 概念涨跌、成分映射 | 命中概念 Top3 |
| `recommend` | 个股推荐/板块推荐服务 | 榜单标签 | 「在某榜」仅作标签，不改购买指数 |

### 3.3 宏观与催化

| 域 ID | 来源 | 关键指标 | 关键信息 |
|-------|------|----------|----------|
| `news` | `intel_cache` 快讯 | 近 3/14 日条数、利好/利空比 | 标题摘要 |
| `policy` | `official_policy` | 近 7/30/180 天条数，国内/国外，类型 | 标题、国家、doc_type、波及板块 |
| `hot_terms` | `hot_term` | 热度、升、降 | 词→板块 |
| `calendar` | 事件日历/展望 | 未来 7 日 ≥4 星事件数 | 事件名+区域 |
| `announce` | 公司公告 | 近窗公告数（有代码才挂个股） | 标题 |

个股与宏观的关联只允许：

1. 板块名命中（行业/概念 ⊆ 政策/热词/快讯 `affected_sectors`）  
2. 公告里的代码  
3. 策略/热词下钻已有的个股 TOP20 接口  

禁止用模糊公司名「猜」代码。

### 3.4 外盘与商品

| 域 ID | 来源 | 关键指标 | 关键信息 |
|-------|------|----------|----------|
| `global` | 全球指数缓存 | 美股/欧股/亚太主要指数涨跌 | 风险偏好一句 |
| `commodity` | 商品行情 + 关联个股 | 原油/黄金等涨跌 | 关联板块名（已有映射则用，无则缺失） |

### 3.5 规则与提醒

| 域 ID | 来源 | 关键指标 | 关键信息 |
|-------|------|----------|----------|
| `strategy_hit` | `strategy.collect_hits` | 买/卖方案 ID 列表 | 「买点方案A·…」全文标签 |
| `alerts` | 智能提醒日志 | 最近命中 | 仅展示，不重复计分 |

---

## 4. 特征与快照表（实现阶段建，本轮只定结构）（FR12-03）

> 本轮 **不执行 DDL 写入业务数据**。下轮实现时一次性建表。字段如下，避免实现时再争论口径。

### 4.1 `engine_run`

一次引擎拍（盘中可多次，盘后必有一次日终）。

| 字段 | 含义 |
|------|------|
| `run_id` | 主键 |
| `asof` | 北京交易日 YYYY-MM-DD |
| `kind` | `intraday` / `eod` |
| `started_at` / `finished_at` | 时间 |
| `status` | `ok` / `partial` / `fail` |
| `domains_json` | 各域 `{available, rows, asof, error}` |
| `brief_json` | `EngineBrief` |
| `note` | 人读失败原因 |

`partial` 允许下游使用，但 UI 必须列出缺失域。

### 4.2 `engine_stock`

`PRIMARY KEY(run_id, code)`

标准化维度（均为 0–100 或 null）：

| 维 ID | 映射 | 缺省 |
|-------|------|------|
| `d_buy` | 购买指数原值 | null（不是 50） |
| `d_sent` | 情绪温度 | null |
| `d_dark` | 暗盘 | null |
| `d_stab` | 企稳分；未过闸门 null | null |
| `d_flow` | 主力净流入+买比合成 | 按现 `_flow_dim`，无资金则 null |
| `d_vol` | 量比映射 | 无量比 null |
| `d_fin` | A100/B75/C45/D15 | **无评级 = 50 且 `fin_missing=1`** |
| `d_sector` | 行业资金映射 | 无行业 null |
| `d_macro` | 政策/热词/快讯命中强度 | 无命中 = 0（不是缺失；0 表示「窗口内没挂上」） |
| `d_ext` | 商品+全球对应该股关联强度 | 无映射 = 0 |
| `d_env` | 市场体制环境分（全市场同一值） | 体制失败则 null |

附加 JSON：`hits`（策略方案）、`catalysts`（政策/热词 ID）、`boards`、`reason_bits`（生成理由的零件）。

**注意**：`d_fin` 进入智能选股综合分，**永不写回** `buy_index`。

### 4.3 `engine_sector` / `engine_catalyst`

板块一行、催化一事。供「宏观催化」「引擎快照」页直接渲染，不再现场聚合。

---

## 5. 分析逻辑（FR12-04）

全部主路径为 **规则**。LLM 只允许写「Brief 的自然语言段」，且受现有 AI 开关/日上限约束；默认 **关闭**。

### 5.1 市场体制 `MarketRegime`

输入：指数涨跌、上涨家数比、成交额较近 5 日、全市场情绪均温。

| 条件（示意） | 体制 |
|--------------|------|
| 上涨家数比≥0.62 且主要指数>0.8% 且量能≥近5日 | `偏多` |
| 下跌家数比≥0.62 且主要指数<-0.8% | `偏空` |
| 其它 | `震荡` |

环境分 `d_env`：偏多 70、震荡 50、偏空 30，再用情绪均温微调 ±10，夹到 0–100。  
缺指数或缺家数：体制 = `未知`，`d_env=null`，智能选股权重里环境维自动跳过（分母不含它）。

### 5.2 板块轮动 `SectorRotation`

- 行业：现有 `hot_score` 口径（涨跌 + 5 日 + 净流入），**有日账本才谈多日，禁止涨跌幅填洞**。  
- 叠加热词：若板块名出现在近两周热词关联里，打 `hot_term` 标签。  
- 叠加政策：近 30 天 `official_policy.affected_sectors` 命中则打 `policy` 标签。  
- 输出 Top 流入 / Top 流出各 20，供智能选股「宏观催化」点击下钻个股 TOP20（复用已有板块个股接口）。

### 5.3 宏观催化 `MacroCatalyst`

合并三类，去重后按 `impact_level`、时间排序，窗口默认 **14 天**（政策可切 30/180 只展示不进个股 `d_macro` 以免过旧）：

1. 官方政策（国内/国外、国家、类型）  
2. 热词（升/降）  
3. 日历 ≥4 星未来 7 日  

`d_macro` 个股算法（实现阶段写死在引擎，不进浏览器）：

```
base = 0
+ 15 × min(3, 命中热词数) 
+ 10 × min(3, 命中近14日政策板块次数)
+ 8  × min(2, 未来7日高星日历且板块相交)
方向：利空条目将该项记负，最后映射到 0–100（50 为中性）
```

无命中 → 0 维值且理由不提宏观（避免「宏观 50」被理解成中性利好）。

### 5.4 个股向量与综合分

智能选股综合分（消费引擎后）：

```
综合分 = Σ (d_i × w_i) / Σ (w_i 且 d_i 非 null)
```

默认权重（相对 11.0 扩展，可在引擎配置里改 **引擎默认**，智能选股页仍可临时覆盖）：

| 维 | 默认 w | 说明 |
|----|--------|------|
| d_buy | 18 | 购买指数 |
| d_flow | 14 | 主力资金 |
| d_fin | 12 | 财报（独立） |
| d_sector | 10 | 板块资金 |
| d_macro | 10 | **新增** 政策/热词/日历 |
| d_vol | 8 | 量能 |
| d_stab | 8 | 企稳 |
| d_dark | 7 | 暗盘 |
| d_env | 7 | **新增** 市场体制 |
| d_ext | 6 | **新增** 商品/全球关联 |

硬性条件仍在选股页：ST、必须净流入、必须有财报、必须企稳、必须量比、必须行业流入；**新增**：

- 必须命中至少 1 个买点方案（读 `SignalBundle`）  
- 必须存在宏观催化（`d_macro>50`）  
- 必须非卖点池

### 5.5 信号包 `SignalBundle`

直接调用现有 `strategy.collect_hits("buy"|"sell")`，把 `plan_id` 写入 `engine_stock.hits`。  
智能选股「策略命中」页 = 引擎快照里的买/卖池，而不是再跑一套 SQL。  
设置→选股策略 仍然是 **启用哪些 A–H** 的地方；引擎只读取启用集。

### 5.6 关键信息提取（给人看 / 给 LLM）

`EngineBrief` 固定结构，便于以后抽字段做分析，而不是一段散文：

```json
{
  "asof": "2026-08-15",
  "regime": {"label": "震荡", "temp": 61, "volume": "平", "sentence": "…"},
  "key_facts": [
    {"type": "policy", "title": "…", "scope": "国内", "country": "中国", "sectors": ["新能源"]},
    {"type": "hot_term", "term": "算力", "heat": 80, "trend": "上升"},
    {"type": "sector", "name": "电子", "net_in_yi": 12.3}
  ],
  "missing": ["holders", "finance_coverage"],
  "disclaimer": "量化参考，不构成投资建议"
}
```

`key_facts` 上限 30 条。AI 点评若开启，只允许吃 Brief + 选股结果前 15 只，禁止把全市场向量塞进提示词。

---

## 6. 更新频率（FR12-05）

与现有调度对齐，**新增一个引擎任务，不替代** 快讯/资金/指标任务。

| 任务 | 默认 | 可调 | 做什么 |
|------|------|------|--------|
| 现有快讯 / 板块资金 / 快照 / 指标 | 保持 | 设置→系统维护 | 继续写原表 |
| 官方政策增量 / 热词 | 4h / 1h | 已有 | 继续写原表 |
| **引擎盘中刷新** | 交易时段 **10 分钟** | 10/15/30 | Collector+Analyzer，`kind=intraday` |
| **引擎日终** | 交易日 **15:50**（指标重建之后） | 定点 | `kind=eod`，作为当日正式快照 |
| 引擎 LLM 摘要 | 默认关 | 随智能选股 AI 策略 | 只改 Brief.sentence |

盘中刷新允许 `partial`。日终若核心域（quote/metrics）失败，`status=fail`，智能选股显示原因并允许回退现场计算。

---

## 7. 设置 → 策略引擎配置（FR12-06）

### 7.1 入口

设置二级菜单：

`系统维护 | 选股策略 | **策略引擎配置**`

- **系统维护**：源健康、缓存、同步、任务频率（不变）  
- **选股策略**：买/卖方案 A–H 启用（不变，红买绿卖）  
- **策略引擎配置**：引擎本身（本轮展示蓝图 + 配置面，控件禁用）

### 7.2 页面信息架构

```
[状态条] 引擎未启用（设计评审）· 下轮实现后此处显示最近 run_id / asof / partial 域
[采集源]  域开关网格（默认全开，本轮 disabled）
[频率]    盘中 10 分钟 / 日终 15:50（本轮 disabled）
[默认权重] 10 维滑条（本轮只展示设计值）
[消费开关] 智能选股综合分接入向量 / 策略命中页走信号包 / 宏观催化页走 Brief
[铁律]     财报不并入购买指数 · 不编造 · SQL 不进浏览器 · LLM 可关
[数据流]   采集→提取→分析→快照→智能选股
[文档]     指向仓库《需求优化文档12.0.md》
```

本轮交互：只读。保存按钮标注「下轮实现后可保存」。禁止把禁用开关的点击写成已生效。

### 7.3 配置存储（实现阶段）

`kv_meta.engine_config` JSON：

```json
{
  "enabled": false,
  "domains": {"market": true, "quote": true, "...": true},
  "intraday_minutes": 10,
  "weights": {"d_buy": 18, "d_flow": 14},
  "consume": {"smartpick_vector": true, "smartpick_signals": true, "smartpick_catalyst": true},
  "llm_brief": false
}
```

`enabled` 默认 `false`，直到实现阶段验收通过再允许打开。打开前智能选股必须仍能用 11.0 现场计算。

---

## 8. 智能选股菜单重设计（FR12-07）

### 8.1 原则

智能选股从「一个带滑条的筛选器」改为 **引擎结果工作台**。  
11.0 的模板/硬条件/权重/AI 点评 **保留在「综合选股」**，避免本轮丢掉能用的功能。

### 8.2 二级结构

```
智能选股
├── 综合选股     现有模板+条件+权重+结果表（本轮仍走 smartpick.run）
├── 策略命中     消费 SignalBundle：买点池 / 卖点池（本轮占位）
├── 宏观催化     消费 MacroCatalyst：政策/热词/日历 → 板块 → 个股 TOP20（本轮占位）
└── 引擎快照     消费 MarketRegime + Brief + 域新鲜度（本轮占位）
```

底部智能提醒条保留，仍读现有 alerts（它已经用选股策略 A–H）。

### 8.3 综合选股（保留并预留接入）

页头增加一句状态：

- 引擎未启用：`综合分来自现场计算 · 策略引擎接入后将改为消费 StockVector`  
- 引擎已启用（实现后）：`综合分来自 15:50 快照 run_xxx · 缺失域：股东`

模板增加（实现阶段，本轮只在设计里登记，**不改 TEMPLATES 代码**以免空跑）：

| 模板 ID | 名称 | 含义 |
|---------|------|------|
| `engine_balance` | 引擎均衡 | 用 §5.4 十维默认权重 |
| `engine_policy` | 政策催化 | 提高 d_macro，硬性 d_macro>50 |
| `engine_signal` | 买点共振 | 硬性命中至少一个买点方案 |
| `engine_rotate` | 板块轮动 | 硬性行业在轮动流入 Top20 |

现有均衡/吸筹/突破/企稳/板块共振/自定义全部保留。

结果表增列（实现阶段）：`策略命中`、`催化`、`体制`。本轮综合表不改列，以免空列误导。

### 8.4 策略命中页（设计）

- 切换：买点（红）/ 卖点（绿）  
- 来源说明：设置→选股策略当前启用的方案  
- 表：与买点窗同一套字段（方案全文、建议、板块、五行、个股热度、板块热度）  
- 空态：`策略引擎未产出信号包。当前可到设置→选股策略查看启用方案，或看综合选股。`  
- **禁止** 本轮用 alerts 接口冒充「引擎已跑」。

### 8.5 宏观催化页（设计）

三块：

1. 关键政策（时间、国内国外、国家、类型）  
2. 热词瓦片（热度/升/降）  
3. 未来 7 日高星日历  

点击路径与宏观情报页一致：词 → 板块 → 个股 TOP20。  
本轮只放布局与「等待引擎 Brief」说明，可提供跳转：`去宏观情报查看原始数据`（官方政策信息 / 热度词汇），避免用户以为功能消失。

### 8.6 引擎快照页（设计）

- 体制卡、量能、情绪、缺失域列表  
- `key_facts` 列表  
- 域覆盖表：quote / metrics / finance / policy / hot_terms / … 的条数与 asof  
- 空态：`尚无引擎快照。设计见设置→策略引擎配置。`

### 8.7 AI 点评

继续：先名单、后模型；关闭/手动/自动；日上限。  
实现接入后，提示词增加 Brief 的 `key_facts` 前 8 条，仍禁收益承诺。本轮不改点评接口。

---

## 9. 接口草案（实现阶段，本轮只提供蓝图读取）

| 接口 | 阶段 | 说明 |
|------|------|------|
| `GET /api/engine/blueprint` | **本轮** | 只读设计对象，供设置页/占位页渲染 |
| `GET /api/engine/config` | 下轮 | 读 `engine_config` |
| `POST /api/engine/config` | 下轮 | 写开关/权重；`enabled` 需校验快照存在 |
| `POST /api/engine/run` | 下轮 | 手动拍一次；设置页按钮 |
| `GET /api/engine/status` | 下轮 | 最近 run、域新鲜度 |
| `GET /api/engine/brief` | 下轮 | `EngineBrief` |
| `GET /api/engine/stocks` | 下轮 | 过滤后的 StockVector 列表 |
| `GET /api/engine/signals` | 下轮 | 买/卖池 |
| `GET /api/engine/catalysts` | 下轮 | 政策/热词/日历 |
| `POST /api/smartpick/run` | 下轮改 | `use_engine=true` 时走向量；否则现场计算 |

本轮 **不得** 实现 `engine/run` 以免空跑被当成已上线。

---

## 10. 与其它模块的边界

| 模块 | 引擎做什么 | 不做什么 |
|------|------------|----------|
| 购买指数 | 只读取 | 不改公式、不并入财报 |
| 选股策略 A–H | 读取启用集与命中 | 不在引擎里动态拼 SQL |
| 宏观情报页 | 仍是原始浏览与下钻 | 引擎只抽关键信息，不替换该菜单 |
| 个股筛选 | 独立 | 可选「代码 ∈ 引擎池」作为以后增强 |
| 看板 | 以后可挂 Brief 一句 | 本轮不改看板 |

---

## 11. 实现顺序（文档评审之后）

| 阶段 | 内容 | 依赖 |
|------|------|------|
| **0 本轮** | 12.0 文档；设置「策略引擎配置」只读蓝图；智能选股四栏信息架构；占位不编数 | — |
| 1 | DDL + Collector（域新鲜度）+ 手动 run | 0 |
| 2 | StockVector + SignalBundle + Brief 规则摘要 | 1 |
| 3 | 智能选股综合分切到向量；策略命中/宏观催化/快照接真数据 | 2 |
| 4 | 盘中 10 分钟 / 日终 15:50 任务；可选 LLM Brief | 3 |

任一阶段缺数都必须 `missing_reason` 可见。

---

## 12. 验收标准

### 12.1 本轮（设计交付）

| 编号 | 验收项 |
|------|--------|
| AC12-D1 | 仓库存在《需求优化文档12.0.md》，含采集域、向量维、频率、智能选股四栏、铁律 |
| AC12-D2 | 设置出现二级菜单「策略引擎配置」，能看懂数据流与配置面，控件标明未启用 |
| AC12-D3 | 智能选股出现 综合选股 / 策略命中 / 宏观催化 / 引擎快照；后三栏无假名单 |
| AC12-D4 | 综合选股原模板与开始选股仍可用 |
| AC12-D5 | 无新的引擎定时任务、无伪造 `engine_run` 数据 |

### 12.2 实现阶段（预告，本轮不测）

| 编号 | 验收项 |
|------|--------|
| AC12-1 | 日终快照含 quote/metrics；缺财报时综合分仍出、未评级为 — |
| AC12-2 | 官方政策与热词能进入 `d_macro` 与催化页，无命中为 0 且不编文件 |
| AC12-3 | 买点池与设置中启用的买点方案一致，不混入卖点方案 |
| AC12-4 | 关闭引擎后智能选股回退现场计算并明示 |
| AC12-5 | 购买指数数值与引擎接入前一致 |

---

## 13. 已知限制

- 新浪 7x24 与专栏翻页无法保证官方政策「文件全文」；引擎只抽已归档标题/摘要。  
- 热词依赖近两周本地情报，情报少则升/降不对称（前窗为空则几乎全是上升）。  
- 散户买卖、主力买比仍为估算。  
- 股东/财报覆盖受源站限速，缺失即缺失。  
- 全部输出为量化参考，不构成投资建议。
