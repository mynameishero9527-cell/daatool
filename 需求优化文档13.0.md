# A股量化工具 — 13.0 需求优化文档

> 本轮主题：**从「能看、能筛、能提醒」升级到「可验证的信号与研究闭环」**。对照当前已交付能力、12.0 策略引擎（仍为设计未跑数），以及「智能量化交易工具」常见产品结构（数据与策略分离、因子框、回测网关、基于委托的止盈止损、本地模拟盘），整理可增强本项目的功能、设计、业务逻辑与策略思路。
>
> **本轮交付范围**：完整优化需求（本文档）。**本轮明确不写业务代码、不接下单网关、不编造 tick/回测成绩。**

- **版本**：v13.0
- **整理日期**：2026-08-15
- **上游文档**：需求文档 1.0、产品优化文档、需求优化文档 2.0～12.0
- **编号约定**：本版新增 **FR13-xx**；缺口 **GAP13-xx**；本轮只验收文档项 **AC13-Dx**

---

## 0. 材料来源与对照结论（必读）

### 0.1 用户指定的三份文件

用户要求阅读目录「量化宝」中的：

1. `量化宝A股智能量化交易工具产品文档.md`
2. `核心功能模块详细功能列表说明书.md`
3. `详细核心代码逻辑实现说明.md`

**本仓库、本运行环境、当前 GitHub 仓库 `mynameishero9527-cell/daatool` 内均未找到这三份文件。** 未假装已读原文，未用空文档编造「量化宝」产品条款。

若随后把这三份文件放进仓库（建议路径 `docs/量化宝/`），下轮按原文逐条修订本文，冲突时以那三份为准覆盖 §3 之后的条目。

### 0.2 实际对照材料

| 材料 | 用途 |
|------|------|
| 当前代码（至 12.0.8：看板/选股策略 A–H/智能选股/持股构成/宏观/价格高亮） | 判断「已有 / 可增强 / 缺失」 |
| 《需求优化文档12.0.md》 + `engine_blueprint.py`（`STATUS=design_only`） | 引擎采集→提取→快照仍未跑数，是 13.0 的 P0 |
| 开源量化宝 bf：`量化宝bf设计文档-草案.docx`、`量化宝bf开发计划.docx`、`量化宝bf初衷.txt`、`量化宝tasks招标.txt`、`数据收集和交易策略的分离.txt`、`数据流程.txt`、`关于数据收集和整理的一点说明.txt`、`2016.4.23关于下一步的开发方向.txt` | 抽取**可迁移的架构与业务分层**，不是搬 CTP 期货实盘 |

上述材料来自公开仓库 `lianghuabao/lianghuabao` 的 `doc/`，**不是**用户点名的三份 A 股产品 md。下文凡写「量化宝」均指这套 bf 设计，避免与未到库的三份原文混淆。

### 0.3 产品定位对照：量化宝草案的三个方案

量化宝 bf 设计草案明确投了三套产品形态：

| 方案 | 原意 | 与本工具关系 |
|------|------|----------------|
| 方案一 | 标的固定、策略择时；商品/分级 B；多账号多策略上云实盘 | **不搬**。本工具无券商网关 |
| 方案二 | 标的不固定：选标策略低频 + 择时策略监控一篮子；可对冲 | **部分迁**：选股（A–H / 引擎池）= 选标；盘中提醒 = 择时监控。对冲/多账户执行不搬 |
| **方案三** | **专注监控和择时，出交易提醒，然后手工交易 / 卖信号** | **本工具就是这一型**。13.0 把「提醒名单」升级成可验证的信号任务，仍不接下单 |

草案原句可直接当本产品边界：「信号系统和执行系统，可以分开；信号只发出来，具体用了几个账户买的，信号系统可以不关心。」本仓库只做信号系统。

量化宝还写明：**全市场扫描选股运算量大，最好做成单独软件**。本工具的引擎扫描 + 智能选股，就是那块「单独软件」；个股分析页只消费池与快照，不在个股页重扫全市场。

### 0.4 从量化宝 bf 抽到、且适合本项目的思想

量化宝 bf 是期货 CTA + CTP 网关，**不能**原样接到本工具（无券商、无 tick 库、A 股 T+1）。可迁移的是：

| 量化宝 bf 原句/结构 | 迁到本工具 |
|---------------------|------------|
| 四层：网关收发、数据服务汇总历史、交易软件子账号/风控/K线/手动、自动化层语言无关 | 本工具压缩为：数据源网关（已有腾讯/新浪）→ 本地库/datafeed → 研究台（看板/个股）→ 策略引擎/信号层。无「交易软件子账号」层 |
| `bfdatafeed` 与 `cta` 库分离：行情库 vs 交易/研究库 | `quant.db` 里快照/K 线是行情库；`engine_*` / `factor_daily` / `signal_task` 是研究库，策略禁止写行情表 |
| 数据收集与交易策略分离：策略不要 `insertTick/insertBar`，历史只读 datafeed | 策略只读本地表/引擎快照，禁止策略函数里打行情 HTTP |
| 收盘后整理 datafeed；盘中策略先补 K 再推增量 | 日终引擎快照 = 正式 datafeed；盘中 10 分钟 = 增量拍 |
| 日线/周线收盘后算好，第二天当过滤器参数不再变 | 体制分、周线门、方案 I/J 用日终截面；盘中增量拍不得改写昨日日终拍 |
| `dataframe.appendBar` 同时算 MACD/均线；大周期 K 由 m1/d1 合成 | 日 K `FactorFrame`：补 bar 时增量算因子，禁止未来函数；周线由日 K 合成，缺则跳过门控 |
| 状态策略 → 交易策略 → 开仓任务 → 成交后生成平仓任务 | 研究端：体制过滤 → 方案 A–H 命中 → **信号任务** → 命中后挂 **止盈/止损提醒**（不是报单） |
| 止损/条件单是独立任务，基于 order 不是基于仓位百分比空转 | 每条买点命中生成独立 `signal_task`（入场价、止盈、止损、有效期） |
| 回测网关 `btgateway`：回测与实盘只换数据源/交易接口 | **日 K 回放网关**：用已落库 K 线，T+1 撮合；禁止用当日收盘价当开盘买入；无 tick 则不做 tick 回放 |
| 策略参数写死在策略里、按策略名跟踪订单 | 方案 SQL 继续只在服务端硬编码；信号带 `plan_id` |
| 一个机器人 = 策略模板 + 行情品种 + 交易品种；多策略靠复制而不是一个进程扫全市场 | 一个方案 = 一段硬编码 SQL；全市场扫描只在引擎 Collector，结果进信号包 |
| 云端 CTA 库只留少量近期 tick/bar，百年级数据留在回测库 | 盘中只用最新快照；回测必须另有 `factor_daily` 历史截面，缺则关闭回测入口 |
| 风控：连续亏损、亏损达阈值、禁止异常单 | 研究组合：单只/单行业上限、卖点剔除、大解禁可选剔除；不做真实平仓 |

### 0.5 明确不从「交易工具」搬过来的部分

| 不搬 | 原因 |
|------|------|
| CTP/QMT/券商下单、撤单、持仓查询 | 本工具是研究与提醒，不是交易通道 |
| Tick 回放、1 分钟双均线实盘 | 本地无 tick 库，禁止用涨跌幅伪造分时成交 |
| 多账号、云端策略容器、远程平仓 | 超出产品边界 |
| Level2 / 委托队列打板自动买入 | 无数据源，禁止编造盘口 |

---

## 1. 当前产品定位 vs 13.0 目标

### 1.1 现在已经能做什么

研究与决策辅助已经较厚：行情、K 线、购买指数/情绪/暗盘/企稳、买/卖方案 A–H、智能选股现场加权、宏观政策/热词、持股与解禁、板块资金账本、AI 回退本地规则。

### 1.2 和「智能量化交易工具」相比缺什么（GAP13）

| ID | 缺口 | 现状 | 13.0 怎么补（研究侧） |
|----|------|------|------------------------|
| GAP13-1 | 没有一层「冻结的数据拍」 | 各页现场 SQL；12.0 引擎未跑 | **先落地 12.0 引擎** |
| GAP13-2 | 策略与采数耦合风险 | 提醒/买点窗直接 `collect_hits` | 命中写入信号包，下游只读快照 |
| GAP13-3 | 无法验证「买点第二天怎样」 | 只有当日命中名单 | **信号跟踪**：+1/+5/+20 日收益，用后续日 K，禁止用命中当日收盘冒充 |
| GAP13-4 | 无回测 | 无 | 日 K 回放，T+1 下一根开盘（无开盘则用下一根收盘并标注 `fill=close`） |
| GAP13-5 | 止盈止损只是文案 | `hit_action` 字符串 | 结构化 `signal_task`：触发价来自命中时快照价 |
| GAP13-6 | 股东/解禁未进策略 | 持股页已有十大/机构/基金/解禁 | 新方案 I/J |
| GAP13-7 | 多周期只在个股页看图 | 方案全是日频快照字段 | 周线趋势过滤（用已有日 K 聚合或周 K，缺则该过滤跳过） |
| GAP13-8 | 无组合约束 | 买点窗可同时很多行业 | 研究组合：单行业上限、ST 已排除、总建议仓位≤100% |

### 1.3 一句话目标

**把已有指标变成可回放、可跟踪、可解释的信号流水线**；智能选股消费引擎快照；买/卖点从「名单」变成「带入场价与后续验证的任务」。仍然 **不构成投资建议、不下真单**。

---

## 2. 不可破的产品铁律（沿用并加一条）

1. 财报评级 **不** 并入购买指数。  
2. 禁止用涨跌幅伪造多日资金。  
3. 无财报评级不得显示为 A。  
4. 策略 SQL **只** 在服务端硬编码。  
5. LLM 失败回退本地规则，不得假装成功。  
6. 缺数展示原因，禁止编造政策全文、热度、股东人数、盘口、成交。  
7. **新增**：回测/跟踪收益必须标明样本、区间、撮合假设；禁止无样本的「胜率 80%」营销数字。  
8. 全部结论为量化参考，不构成投资建议。

---

## 3. 总体架构（FR13-01）

沿用 12.0 四层，并在快照之后加「验证层」。策略进程 **禁止** 写行情、禁止 insert bar。

```
现有调度（腾讯/新浪/东财 F10）──写入──► 本地库
                                      │
                            ① Collector  只读库，记域新鲜度
                                      │
                            ② Extractor  关键指标 + 短事实，空值保留
                                      │
                            ③ Analyzer   体制 / 轮动 / 催化 / 向量 / 信号包
                                      │
                            ④ Snapshot   engine_run / engine_stock（日终正式拍）
                                      │
                            ⑤ Validator  信号跟踪 + 日K回放（只读历史K）
                                      │
              ┌──────────┬────────────┼────────────┬──────────┐
              ▼          ▼            ▼            ▼          ▼
         智能选股    买/卖点窗     跟踪报表     模拟组合    AI Brief
```

核心代码边界：

| 模块（实现阶段） | 职责 | 禁止 |
|------------------|------|------|
| `app/services/engine_collect.py` | 域扫描、asof、条数 | HTTP |
| `app/services/engine_analyze.py` | 体制、向量、催化 | 改 `buy_index` |
| `app/services/engine_store.py` | 写 `engine_run` 等 | 覆盖历史日终拍（只替换当日） |
| `app/services/factor_frame.py` | 按 code 追加日 K，算 MA/MACD/RSI | 用未来 bar |
| `app/services/backtest.py` | 按交易日回放方案命中 | 用命中当日收盘当买入价（默认下一根） |
| `app/services/signal_track.py` | 命中后复权收益 | 没有后续 K 就填 null + 原因 |
| `app/services/paper.py` | 本地 T+1 模拟账本 | 连接券商 |
| `strategy.py` | 仍只提供硬编码 SQL | 从浏览器接 SQL |

`engine_blueprint.py` 在实现阶段从 `design_only` 改为读 `kv_meta.engine_config`，默认 `enabled=false`，打开前智能选股必须仍能现场计算。

---

## 4. P0：把 12.0 引擎跑起来（FR13-02）

12.0 已定域、向量维、频率、智能选股四栏。13.0 **不再改口径**，只要求实现：

1. DDL：`engine_run` / `engine_stock` / `engine_sector` / `engine_catalyst`（字段以 12.0 §4 为准）。  
2. 手动「跑一次引擎」+ 日终 15:50 + 盘中 10 分钟（可关）。  
3. 智能选股：综合分可切向量；策略命中/宏观催化/快照接真数据；关引擎则回退并明示。  
4. `SignalBundle` = `strategy.collect_hits`，买/卖不混。  
5. `d_macro` 无命中为 0，不编政策。

验收预告见 AC13-1～AC13-5（实现阶段）。无快照时禁止用随机名单填「策略命中」页。

---

## 5. 因子框 FactorFrame（FR13-03）

### 5.1 动机

现在 MACD/均线/RSI 在指标重建时算在 `stock_metrics` 上，是 **截面快照**。回测需要 **时间序列**，否则会用「今天的指标」判断「昨天该不该买」。

### 5.2 逻辑

对单只股票的日 K 数组（已落库、按日期升序）：

```
append_bar(o,h,l,c,v,date)
  → 更新 close[] 
  → 重算最新一根的 ma5/10/20、macd、rsi14、bias20、pos60、量比（相对已发生的均量）
peek(i) 只允许 i≤last
```

规则：

- 第 t 日因子只用 `bars[0..t]`。  
- 全市场回测时因子可日终批量落 `factor_daily(code, date, ...)`，避免每次回放重算。  
- 缺 K 线的股票该日不产生信号，不拿快照价硬凑 OHLC。

### 5.3 与现有 `stock_metrics` 的关系

盘中提醒仍可用当日快照指标（与现在一致）。  
回测/跟踪 **必须** 用 `factor_daily` 或 FactorFrame，不得回放时读取「最新一天」的 `stock_metrics`。

---

## 6. 信号生命周期（FR13-04）

把「名单」改成任务，对应量化宝 bf 的 OpenTask / CloseTask，但是 **提醒任务**：

```
体制过滤（可选） → 方案命中 → 生成 signal_task
                              ├─ entry_px = 命中当时快照现价（已展示给用户的那个价）
                              ├─ stop_px / take_px 由方案写死公式
                              ├─ expire = 命中日起 N 个交易日
                              └─ 次日及以后：用日K判断是否触及，只记事件，不下单
```

### 6.1 表 `signal_task`（实现阶段）

| 字段 | 含义 |
|------|------|
| `id` | 主键 |
| `asof` | 命中交易日 |
| `code` | 股票 |
| `side` | buy / sell |
| `plan_id` | A–J 等 |
| `entry_px` | 命中时现价，缺则任务无效 |
| `stop_px` / `take_px` | 可空 |
| `status` | open / hit_take / hit_stop / expired / invalid |
| `exit_date` / `exit_px` | 触发日与价 |
| `note` | 无效原因 |

默认公式（写死服务端，可按方案覆盖）：

- 买点止损：`entry_px × 0.92`（−8%），止盈：`entry_px × 1.12`（+12%），有效 10 个交易日。  
- 卖点不做「开空」（A 股 T+1 研究工具默认只跟踪「若持有则减仓」的假想，不计算融券收益）。

### 6.2 买点窗 / 智能提醒 UI

每条除现价、涨跌幅外增加：

- 入场参考价（即 `entry_px`）  
- 止盈 / 止损参考价（有则显示）  
- 若已有跟踪：`+1日` / `+5日` 收益，无后续 K 显示「待收盘验证」

禁止把止盈止损画成「已成交」。

---

## 7. 日 K 回放网关（FR13-05）

### 7.1 目标

验证方案 A–H（及 I/J）在 **过去区间** 的命中后表现。这是研究功能，不是实盘。

### 7.2 撮合（必须写在页头）

| 项 | 默认 |
|----|------|
| 频率 | 日 K |
| 买入 | 信号日 **下一交易日开盘**；无开盘字段则用下一根收盘并标注 |
| 卖出/结束 | 先触止盈/止损（用当日高低价近似，并在说明里写「高低价触及假设」），否则到期收盘 |
| T+1 | 信号日不能卖出 |
| 费用 | 默认双边万三 + 印花税（卖出 0.1%），可关 |
| 涨跌停 | 一字板无法成交则 **不成交**，计入 invalid，不按收盘强买 |
| 复权 | 用已有 K 线口径；若 K 线未复权须在报表标明 |

### 7.3 输出（缺样本就空）

- 样本量、有后续 K 的条数  
- 平均 +1/+5/+20 日收益  
- 最大回撤（组合等权、每日调仓上限可关）  
- 按 `plan_id` 分组  

禁止：样本 < 30 还展示「年化夏普」；禁止把回测曲线画成实盘资金。

### 7.4 入口

设置 → 选股策略 旁增加只读说明；完整回测页放在 **智能选股 → 信号验证**（新四级或新二级）。  
本轮文档登记，实现放在引擎能出快照之后。

---

## 8. 新策略思路（FR13-06）

现有 A–H 全部保留。新增方案 **服务端硬编码**，默认 **关闭**，由用户在选股策略里启用。

### 8.1 方案 I · 筹码集中（买）/ 户数扩散（卖）

数据：已有持股接口规范化字段（户数环比、机构占比）。缺披露则该股不命中。

- **买**：股东户数环比下降（更集中）∧ 机构占流通上升或机构家数增加 ∧ 主力净流入 > 0 ∧ 非 ST。  
- **卖**：户数环比明显上升 ∧ 机构占比下降。  
- **禁止**：API 失败时用十大股东名单「猜」户数。

### 8.2 方案 J · 解禁避让（卖）/ 解禁后回流（买，更严）

数据：已有限售解禁列表。

- **卖（主）**：未来 10 个交易日内存在解禁且解禁占流通 ≥ 阈值（默认 3%，写死可改常量）∧ pos60 ≥ 0.6。  
- **买（严）**：解禁日已过 ≥ 5 个交易日 ∧ 解禁后区间主力净流入 > 0 ∧ BuyIndex ≥ 65。无解禁数据则不买不卖。

### 8.3 方案 K · 周线过滤（不是独立池，是 A–J 的门）

- 买点额外：周线 MA5 ≥ MA20（用日 K 合成周线或周 K，缺则 **跳过本门** 不当成失败）。  
- 卖点额外：周线 MA5 < MA20 可加强卖点权重，但不单独出池以免过拟合。

### 8.4 研究用、不做自动打板的形态

涨停、开板、封单量：**只在榜单/个股页展示已有字段**。13.0 不新增「打板策略自动买」，避免无盘口数据时编造。

### 8.5 多因子共振标签（非新 SQL 池）

引擎向量上打标签：`hit_count` = 同时命中的买点方案数。智能选股可筛「至少命中 2 个买点方案」。文案用已有 `plan_labels` 全文，不缩成字母。

---

## 9. 研究组合与风控（FR13-07）

不做真仓。提供 **建议组合约束**（智能选股结果页可选）：

| 规则 | 默认 | 缺数 |
|------|------|------|
| 单只建议权重 | ≤ 10% | — |
| 单申万行业合计 | ≤ 30% | 无行业则该股不计入行业约束、单独列出 |
| 建议总仓 | ≤ 100% | — |
| 卖点池股票 | 从买点建议里剔除 | — |
| 未来 10 日大解禁 | 可选剔除（读方案 J 数据） | 无解禁表则本规则跳过 |

仓位建议算法（实现写死）：等权后再按上限切分，剩余权重不重分配到已达上限的行业（避免偷偷加码）。

本地 **模拟账本**（可选、默认关）：

- 用户在结果上点「记一笔模拟买入」，T+1 后才允许记卖出。  
- 价格用当时快照现价，保存 `paper_fill`。  
- 不自动跟策略下单，避免用户以为已接券商。

---

## 10. 核心代码逻辑（实现时按此写）

### 10.1 日终引擎（伪代码）

```
asof = 北京交易日
if 核心域 quote/metrics 不可用:
    写 engine_run(status=fail, note=原因); return
domains = collect_all()          # 每域 available/rows/asof/error
regime = market_regime()         # 缺指数 → 未知, d_env=null
rotation = sector_rotation()     # 无账本则多日资金维缺失
catalysts = macro_catalysts(14d)
hits_buy = collect_hits("buy")
hits_sell = collect_hits("sell")
for code in 当日快照:
    vec = map_dimensions(...)    # null 保留; d_fin 无评级=50+fin_missing
    vec.hits = plans for code
    vec.d_macro = score(catalysts ∩ boards)
    upsert engine_stock
写 engine_run(status=ok|partial)
对当日 hits 生成/更新 signal_task（无现价则 invalid）
```

### 10.2 跟踪（每个交易日指标重建之后）

```
for task in open tasks:
    bars = kline(code) after asof
    if 无下一根: continue
    # 不在 asof 当天卖
    for bar in bars[1:]:
        if side==buy and bar.low <= stop: close hit_stop
        if side==buy and bar.high >= take: close hit_take
        if 超过 expire: close expired at bar.close
```

高低价同时触碰止盈止损时：**记 invalid_ambiguous**，不选边讨好收益。

### 10.3 回放（禁止未来函数）

```
for date in [start, end]:
    用 date 当天及以前的 factor_daily 跑 collect_hits 的「历史重放」
    历史重放不得调用「最新 stock_metrics」
    成交发生在 next_open(date)
```

历史重放需要 `metrics_daily` 或等价截面；若只有最新截面，**本功能保持关闭** 并在 UI 写「缺少按日因子表，无法回测」。这是硬门槛，避免假回测。

因此 13.0 实现顺序必须是：引擎日终落截面 → 再打开回测。没有按日截面时，只做「命中后跟踪」（用后续真实 K 线），这不需要重放历史规则。

---

## 11. 页面与信息架构（FR13-08）

### 11.1 智能选股（在 12.0 四栏上加一栏）

```
智能选股
├── 综合选股     11.0 现场计算 → 引擎启用后改向量
├── 策略命中     信号包 + 现价 + 入场参考 + 止盈止损参考
├── 宏观催化     Brief
├── 引擎快照     体制/域新鲜度
└── 信号验证     跟踪表 +（有按日截面才开放）回测  ← 新增
```

### 11.2 设置

```
系统维护 | 界面外观 | 选股策略 | 策略引擎配置
```

策略引擎配置：12.0 控件在实现后解除 disabled；增加「生成按日因子」开关（默认关，避免盘中全市场重算）。

### 11.3 个股分析

K 线旁可用已有指标；增加只读「若纳入买点任务：入场价/止盈/止损」。无任务则不显示，避免每只股票都假造一条。

---

## 12. 与 12.0 / 11.0 的关系

| 文档 | 13.0 态度 |
|------|-----------|
| 12.0 引擎 | **全部采纳，作为 P0 实现**，不改向量公式 |
| 11.0 智能选股 | 综合选股保留；新栏不替代它 |
| 方案 A–H | 保留；I/J 默认关闭 |
| 持股 12.0.6～12.0.8 | 现价高亮、持仓构成、买点现价保留；I/J 消费其数据 |

---

## 13. 开发顺序

| 阶段 | 内容 | 依赖 |
|------|------|------|
| **0 本轮** | 本文档评审 | — |
| 1 | 落地 12.0 引擎 DDL + 手动/日终 run + 智能选股三栏接真数据 | 0 |
| 2 | `signal_task` + 买点窗展示入场/止盈止损 + 后续 K 跟踪 | 1 |
| 3 | 方案 I/J（缺持股/解禁则空）+ 组合约束 | 1 |
| 4 | 按日因子表 + 日 K 回放（无因子表则入口保持关闭并写原因） | 1、2 |
| 5 | 可选本地模拟账本（T+1，默认关） | 2 |

任一阶段禁止用假胜率、假成交、假政策凑页。

---

## 14. 验收标准

### 14.1 本轮（文档）

| 编号 | 验收项 |
|------|--------|
| AC13-D1 | 仓库存在《需求优化文档13.0.md》，写明指定三份量化宝 md 未在库内；对照了 12.0 引擎蓝图，以及量化宝 bf 设计草案/开发计划/数据流程（方案三=提醒型产品） |
| AC13-D2 | 写清 P0=实现 12.0 引擎；信号任务、跟踪、回测、I/J、模拟盘的边界与铁律 |
| AC13-D3 | 明确不做券商下单、tick 回放、用涨跌幅伪造资金/盘口 |
| AC13-D4 | 本轮无新的引擎定时任务、无伪造回测曲线 |

### 14.2 实现阶段（预告）

| 编号 | 验收项 |
|------|--------|
| AC13-1 | 日终 `engine_run` 存在；缺财报时未评级为 —，购买指数与接入前一致 |
| AC13-2 | 买点池与设置中启用买点方案一致；现价与快照一致，缺价不编 |
| AC13-3 | 跟踪收益只用后续日 K；无下一根则空而不是 0% |
| AC13-4 | 高低价同日触碰止盈止损记 ambiguous，不挑选有利一侧 |
| AC13-5 | 无按日因子时回测入口关闭并说明原因 |
| AC13-6 | 方案 I/J 在持股/解禁接口失败时零命中，不猜股东人数 |

---

## 15. 已知限制

- 无 tick、无 Level2，回测触及止盈止损只能用日高低价近似。  
- 快照资金、散户买卖仍为估算。  
- 历史重放需要按日截面，存储与计算量大于「只跟踪未来」。  
- 官方政策仍无稳定全文源，催化只基于已归档标题/摘要。  
- 指定的三份「量化宝」产品 md 补进仓库后，应再出 13.1 勘误，避免架构思想与原文冲突。

---

## 16. 附录：若三份原文到达后的对照清单

把文件放入 `docs/量化宝/` 后，按下列标题核对是否覆盖，未覆盖的补 FR13 子条：

1. 产品文档：定位、用户、范围、非目标、模块图  
2. 功能列表说明书：每个模块字段级功能、权限、刷新频率  
3. 核心代码逻辑：类图、时序、数据表、关键函数、失败回退  

冲突处理：原文要求真·自动交易 → 本工具仍只做信号与模拟，把交易列为「外部通道、本仓库不实现」。原文要求的数据源若本工具没有 → 记 GAP，禁止用相近字段冒充。
