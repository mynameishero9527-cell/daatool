# A股量化工具 — 10.0 需求优化文档

> 本文档基于 9.0 已交付版本与代码核对结果整理。主题三块：
> ① 全市场个股/筛选维度补齐 **财报评级**；
> ② **宏观情报** 全子页问题与未完成细节清单；
> ③ 板块资金新增二级菜单 **板块资金分析**（全 A 板块横向直方图 + 时间范围 + 点选后出个股）。
>
> **开发顺序**：本文档先评审提交，再按本文实现功能与算法细节。

- **版本**：v10.0
- **整理日期**：2026-08-15
- **上游文档**：需求文档 1.0、产品优化文档、需求优化文档 2.0～9.0
- **编号约定**：本版新增 **FR10-xx**；缺陷项 **BUG10-xx**；未完成项 **GAP10-xx**

---

## 1. 代码核对结论（实现前必读）

### 1.1 财报评级现状

| 项 | 现状 |
|----|------|
| 个股分析页 | 有「财务分析」卡：多期营收/净利 + **A/B/C/D 健康评级**（`GET /api/finance`） |
| 评级规则 | 最新期营收同比、归母净利同比、净利率、营收连续正增长期数 → 打分映射 A/B/C/D |
| 落库 | `finance_report` 仅在打开个股时按需拉取；当前库内约 **1 只股票 / 6 期**，无法支撑全市场筛选 |
| 列表/筛选 | 自选、筛选、推荐、榜单、板块成分股、快讯相关个股、AI 选股 **均无财报评级列/条件** |

3.0 曾约定「财务评级不并入购买指数」。10.0 **保持该约定**：评级作为独立展示与筛选维度，不改购买指数公式。

### 1.2 宏观情报子页核对

| 子页 | 已有 | 问题 / 未完成 |
|------|------|----------------|
| 实时快讯 | 时间/等级/方向/区域筛选、关注度、评论、板块标签可点 TOP20–50 | 见 BUG/GAP |
| 政策追踪 | 同上（政策口径） | 政策条数依赖快讯识别，源少时列表偏空（口径问题，需标注） |
| 影响评估 | 复用快讯高等级条目 | 与快讯展示高度重复，缺少「仅看 ≥重大」默认强调 |
| 事件展望 | 周/月/季/半年、国内国外分栏、自定义事件 | 自定义事件无影响板块，点开易落到「指数权重」兜底（GAP10-2） |
| 事件日历 | 3 个月/半年、今天～一月/半年、星级 | **「今天」用 `toISOString()`（UTC）**，与北京日可能差一天（BUG10-1） |
| 板块事件 | 日/周/月分组、板块可点 | **周分组同样用 UTC `toISOString()`**（BUG10-1） |
| 公司公告 | 快讯识别 + 个股过滤 + 机构评级 | 非交易所全文，已有口径说明；板块标签已可点 |
| 股票常识 | 分组词条 + 搜索 | 搜索框 placeholder 文案重复（GAP10-3） |

### 1.3 板块资金现状

| 项 | 现状 |
|----|------|
| 二级菜单 | 仅「资金全景」「板块推荐」 |
| 资金全景 | 行业/概念 × 热力图/列表，点方块出成分股；**只有当日** `SUM(main_net_in)` |
| 快照字段 | 个股有 `main_net_in`（当日）、`main_net_in_d5`（近 5 日累计）；**无** 3/10/20/60 日资金累计，也 **无** 板块日频资金历史表 |
| 结论 | 「当天 / 近 5 日」可立刻用快照聚合；「近 3/10/20 日、一月、三月」必须 **落库日频板块资金** 后按交易日累加，不足天数须标明 |

---

## 2. 财报评级维度（FR10-01）

### 2.1 评级定义（沿用 3.0，固化可筛选）

对每只股票用最新一期（及近几期）财报计算 **财务健康评级 A/B/C/D**，无数据为「—」。

| 评级 | 含义（界面短标签） |
|------|-------------------|
| A | 成长较好 |
| B | 基本稳健 |
| C | 增长承压 |
| D | 双双承压 |
| — | 暂无财报 |

计分规则（与现 `finance.py` 一致，写入全市场表）：

- 营收同比：>20% +2，>0 +1，否则 -1
- 归母净利同比：>30% +2，>0 +1，否则 -2
- 净利率：>10% +1，>3% 0，否则 -1
- 营收连续 ≥3 期正增长 +1
- ≥5 → A；≥3 → B；≥0 → C；否则 D

### 2.2 数据落地（FR10-01-1）

| 项 | 设计 |
|----|------|
| 存储 | `stock_list` 增加 `finance_grade`（A/B/C/D/空）、`finance_summary`（一句话）、`finance_updated`；或独立表 `stock_finance_grade(code, grade, score, summary, updated_at)` |
| 计算 | 后台任务「财报评级重建」：对有 `finance_report` 的股票直接评级；无财报的按代码分批拉新浪披露（限速），失败保持「—」 |
| 调度 | 盘后/维护任务每日一次；设置页可手动触发；**不阻塞启动** |
| 购买指数 | **不**把财报评级并入购买指数/综合评分权重（避免低频污染高频） |

首期允许覆盖率不满：界面在筛选组旁标注「已评级 N / 全市场 M」。未评级股票筛选「A」时不出现，选「无评级」可单独筛出。

### 2.3 个股列表展示（FR10-01-2）

以下所有「股票维度」表格/卡片增加 **财报** 列（彩色徽章，与个股页一致：A 红偏暖 / B 橙 / C 灰 / D 绿偏冷）：

| 位置 | 列位置建议 |
|------|------------|
| 行情看板 · 自选股 | 五行之后 |
| 个股筛选结果 | 名称后 |
| 个股推荐 | 五行之后 |
| 榜单 | 五行之后 |
| 板块资金 · 成分股 / 板块推荐个股 | 名称后 |
| 快讯/政策相关个股 TOP20–50 | 标签列内增加财报徽章 |
| AI 选股结果 | 名称后 |
| ETF 近似持仓 | 名称后 |

点击徽章不打断行点击进个股；悬浮显示 `finance_summary`。

### 2.4 筛选维度（FR10-01-3）

| 入口 | 设计 |
|------|------|
| 个股筛选 | 新增平铺组「财报评级」：A / B / C / D / 无评级；组内多选 OR，与其他组 AND |
| 个股推荐 | 二级筛选增加「财报」：全部 / A / B / C / D |
| 语义筛选 / AI 选股 | 关键词：绩优、财报优秀、成长股 → A+B；业绩差、暴雷风险 → D；财务稳健 → B |
| 预设方案 | 新增「财报优质」：评级 A + 剔除 ST |

---

## 3. 宏观情报：问题清单与完善（FR10-02）

### 3.1 必须修复（BUG10）

| 编号 | 问题 | 修复 |
|------|------|------|
| BUG10-1 | 日历「今天/N 天内」与板块事件「按周」用 `Date.toISOString()`（UTC），云主机 UTC 下与北京日错位 | 前端统一用 **北京日历日**（`Asia/Shanghai` 的 YYYY-MM-DD）做比较与周起始 |
| BUG10-2 | 展望/日历点事件若 `sectors` 为空，详情落到「指数权重」大市值股，易误解 | 无板块时展示「未映射影响板块」空态，不拉指数权重兜底 |

### 3.2 应补细节（GAP10）

| 编号 | 项 | 设计 |
|------|----|------|
| GAP10-1 | 政策追踪偏空 | 子页顶部固定口径：「政策条来自 7x24 快讯关键词识别，非政策库全文」 |
| GAP10-2 | 自定义事件无板块 | 添加表单增加「影响板块」可选填（逗号/顿号）；写入后点击按该板块出个股 |
| GAP10-3 | 常识搜索文案 | placeholder 改为「搜索术语，如 量比 / 换手率 / 市盈率」 |
| GAP10-4 | 影响评估与快讯重复 | 影响评估默认筛选「中等及以上」，并在页头说明「高等级快讯专题」 |
| GAP10-5 | 相关个股财报列 | 与 FR10-01-2 对齐，TOP 列表标签含财报评级 |

不改宏观一级信息架构（仍为现有 8 个二级页）。

---

## 4. 板块资金分析（FR10-03）— 新二级菜单

### 4.1 入口与原则

在「板块资金」一级下增加第三枚二级选项卡：

`资金全景` | `板块推荐` | **`板块资金分析`**

本页 **只分析板块总体资金流入/流出**，不做热力图、不做涨跌幅主展示。  
**未点选板块时，底部不出现个股表。** 点选某一板块后，才在 **同一界面底部** 展开该板块个股。

### 4.2 维度

| 控件 | 选项 |
|------|------|
| 板块维度 | **行业板块**（申万一级，全市场约 31 个） / **题材概念**（本地概念映射，过滤泛化标签后） |
| 时间范围 | **当天** / **过去 3 天** / **过去 5 天** / **过去 10 天** / **过去 20 天** / **过去一月** / **过去 3 个月** |

「天」均指 **A 股交易日**。一月按近 20 个交易日，3 个月按近 60 个交易日（与现有 `pct_d20` / `pct_d60` 窗口对齐，避免自然月休市歧义）。界面按钮文案用用户原话，旁注「交易日」。

### 4.3 指标口径

每个板块一条：

| 字段 | 含义 |
|------|------|
| name | 板块名 |
| net_in_yi | 区间内成分股主力净流入合计（亿元），**红入绿出** |
| amount_yi | 区间成交额合计（亿元），若该日无历史成交额则仅用已落库日 |
| stocks | 成分股只数 |
| coverage | 实际参与累计的交易日数 / 目标交易日数（如 4/5） |

**当天**：`SUM(个股 main_net_in)`，与资金全景当日一致。  
**过去 5 天**：优先 `SUM(个股 main_net_in_d5)`（快照已有，可立即交付）。  
**过去 3/10/20 天、一月、3 个月**：对 `sector_flow_daily` 按日期求和。

### 4.4 日频落库（FR10-03-1）

新增表 `sector_flow_daily`：

| 列 | 说明 |
|----|------|
| dim | industry / concept |
| name | 板块名 |
| trade_date | 北京交易日 YYYY-MM-DD |
| net_in | 当日主力净流入（万元） |
| amount | 当日成交额（万元） |
| stocks | 成分股数 |
| PRIMARY KEY | (dim, name, trade_date) |

盘中快照任务成功后 **upsert 当日** 一行；盘后任务再写一次收盘值。  
历史不足时：直方图仍画已有日期的合计，条上或页头标注「已累计 N 个交易日（目标 M），早期日期待落库」。

**禁止**用涨跌幅或成交额冒充「资金流入」；无资金字段的日期不得用价格涨跌填洞。

### 4.5 呈现：横向直方图（FR10-03-2）

- 全部板块一条横条，**按净流入从大到小**（上为流入最多）。
- 条长 = `|净流入|` 相对本页最大绝对值的比例；流入红色向右，流出绿色向右（或流入向右、流出向左的对称轴，实现时选一种并在页头说明）。推荐：**零轴居中，流入向右红、流出向左绿**。
- 左侧板块名，右侧标注 `+12.3亿` / `-4.1亿`。
- 支持按「流入最多 / 流出最多 / 绝对额」排序切换（默认流入最多，流出为负排在下方）。
- 点击某一条：高亮该条，视为「选中该板块维度」。

### 4.6 底部个股（FR10-03-3）

仅当 `selected_sector` 非空时渲染：

| 列 | 说明 |
|----|------|
| 名称、代码 | 点击进个股分析 |
| 涨跌幅 | 红涨绿跌（当日） |
| 购买指数 | 有则显示 |
| 财报评级 | FR10-01 徽章 |
| 五行等标签 | 与快讯相关个股精简列一致 |
| 区间贡献 | 该股在所选时间范围的主力净流入（当天用 `main_net_in`，5 日用 `main_net_in_d5`，更长区间在有个股日频前可先显示当日并标注「个股区间资金仅当天/5日可精确」） |

默认按该股资金贡献排序，TOP 不强制截断（可先 50，提供「显示更多」）。  
取消选中或切换维度/时间后清空个股区。

### 4.7 接口

| 接口 | 说明 |
|------|------|
| GET /api/sector/flow-bar?dim=&range= | range=`1d\|3d\|5d\|10d\|20d\|1m\|3m`；返回 items + coverage + note |
| GET /api/sector/flow-bar/stocks?dim=&name=&range= | 选中板块后的个股列表 |
| POST /api/system/rebuild-finance-grades | 手动重建财报评级（设置页按钮） |

---

## 5. 设置与任务

| 任务 | 说明 |
|------|------|
| 财报评级重建 | 可手动；维护窗口自动 |
| 板块资金日频落库 | 挂在现有快照同步成功之后，可开关 |

设置页「指标/同步」区增加：已评级股票数、板块资金已落库交易日数。

---

## 6. 验收标准

| 编号 | 验收项 |
|------|--------|
| AC10-1 | 筛选器出现「财报评级」组；推荐出现财报筛选；至少自选/筛选/推荐/榜单四张表有财报列 |
| AC10-2 | 无财报股票显示「—」，不误标 A |
| AC10-3 | 宏观日历「今天」与板块事件「按周」按北京日，不再用 UTC 日期 |
| AC10-4 | 无影响板块的事件详情不再出现指数权重兜底股 |
| AC10-5 | 板块资金出现「板块资金分析」；直方图含全行业或全概念；时间 7 档可切 |
| AC10-6 | 未点板块时无个股表；点一条后底部出现该板块个股 |
| AC10-7 | 当天、近 5 日有数；更长区间无历史时有「已累计 N/M 日」说明，不用涨跌幅冒充资金 |
| AC10-8 | 购买指数公式不因财报评级改变 |

---

## 7. 实现顺序（文档提交后）

1. 北京日工具 + 宏观 BUG10 / GAP10  
2. 财报评级落库与后台重建 + 各列表/筛选接入  
3. `sector_flow_daily` + 快照后写入  
4. 「板块资金分析」页：直方图 + 时间档 + 点选个股  

---

## 8. 已知限制

- 财报依赖新浪披露接口，全市场首轮评级耗时较长，允许分批、覆盖率爬升。
- 3/10/20 日及一月、三月资金依赖日频落库，上线当日更长档可能只有 1 日，须明示。
- 概念板块成分随同步变化，历史日按 **当日映射** 聚合（不回算旧成分）。
- 主力净流入为行情源估算字段，非交易所逐笔还原。
