# A股量化工具 — 11.0 需求优化文档

> 本文档基于 10.0 已交付版本与本轮使用反馈整理。主题四块：
> ① **板块资金分析** 直方图「两边都显示」与维度筛选未完成；
> ② **财报评级** 覆盖率、筛选与列表口径仍不完整；
> ③ 新增一级菜单 **智能选股**：综合现有全部数据/指标做精准选股，并可按开关调用 AI 写总结；
> ④ 各类指标形态（档位、区间、权重、开关、自然语言）统一进智能选股。
>
> **开发顺序**：本文档先评审提交，再按本文实现功能与算法细节。

- **版本**：v11.0
- **整理日期**：2026-08-15
- **上游文档**：需求文档 1.0、产品优化文档、需求优化文档 2.0～10.0
- **编号约定**：本版新增 **FR11-xx**；缺陷项 **BUG11-xx**；未完成项 **GAP11-xx**

---

## 1. 代码核对结论（实现前必读）

### 1.1 板块资金分析

| 项 | 现状 | 问题 |
|----|------|------|
| 入口 | 板块资金三级：资金全景 / 板块推荐 / 板块资金分析 | 可用 |
| 数据 | `GET /api/sector/flow-bar` 行业约 31 条、概念约 150 条；当天/5 日可出数 | 可用 |
| 直方图 | **同时**渲染 ECharts 柱状图 + HTML 零轴横条 | **BUG11-1**：同一数据画了两遍，看起来「两边都有图」 |
| ECharts | 数值轴左右双向，标签一律 `position: right` | 流出条标签容易叠在条上，和页头「零轴居中」说明不完全一致 |
| 维度 | 行业/概念、7 档时间、流入/流出/绝对额 | 切换后会清个股；**无板块名搜索、无只看流入/流出过滤、无成分股数下限**（GAP11-1） |
| 个股 | 点选后出表，已含财报/量比/主力散户 | 不能按财报再筛；切换维度后选中态丢失是对的，但缺少「当前选中」提示条 |

10.0 约定「零轴居中：流入向右红、流出向左绿」。11.0 **只保留这一套直方图**，禁止双通道同时展示。

### 1.2 财报评级

| 项 | 现状 |
|----|------|
| 规则 | A/B/C/D 独立于购买指数（保持） |
| 落库 | `stock_finance_grade`；启动 `rebuild_all(max_fetch=0)` 只评已有财报 |
| 覆盖 | 实测约 **1 / 4595**，筛 A / A+B 经常为空 |
| 筛选 | 个股筛选有 A/B/C/D/无评级；推荐有全部/A/B/C/D/A+B/无评级 |
| 列表 | 自选/筛选/推荐/榜单等已有财报列与资金列 |

缺口：覆盖率爬升慢、设置页重建默认只拉 80 只、智能场景无法把「财报 + 资金 + 量化」一次合成。

### 1.3 选股入口分散

| 入口 | 能力 | 缺什么 |
|------|------|--------|
| 个股筛选 | 多条件 AND + 语义句 | 无综合分、无权重、不调 AI |
| 个股推荐 | 固定榜单 + 二级筛 | 不能跨榜单合成 |
| AI 选股 | 一句话 → 语义规则 | 点评失败时无策略；不综合宏观/板块资金 |
| AI 分析 | 大盘/个股/自由问 | 不是选股工作台 |

用户需要 **一个一级菜单**，把上述数据合成「精准选股」，并 **可控地** 调用大模型写总结。

### 1.4 大模型

10.0.2 已有：独立客户端、地址规范化、`POST /api/ai/test`、失败回退本地规则。  
11.0 增加：**调用频率与手动/自动开关**（只作用于智能选股的总结，不改右键单股分析）。

---

## 2. 板块资金分析完善（FR11-02 / BUG11）

### 2.1 直方图只留一套（BUG11-1）

| 项 | 设计 |
|----|------|
| 默认 | **仅 HTML 零轴居中横条**：流入向右红、流出向左绿；左侧板块名，右侧 `+12.3亿` / `-4.1亿` |
| ECharts | 不再默认并排渲染。提供视图切换：`直方图`（默认） / `图表`。同一时间只显示一种 |
| 图表模式 | 若用户切到图表：负值向左、正值向右；标签流入在右侧、流出在左侧；点击同样选中板块 |
| 页头 | 固定一句：「红入绿出 · 零轴居中 · 点选一条后底部才出个股」 |

### 2.2 维度筛选补齐（GAP11-1）

在现有「板块维度 / 时间范围 / 排序」同一行或下一行增加：

| 控件 | 选项 | 行为 |
|------|------|------|
| 板块维度 | 行业板块 / 题材概念 | 已有；切换清空选中与个股 |
| 时间范围 | 当天～过去 3 个月（交易日） | 已有 |
| 排序 | 流入最多 / 流出最多 / 绝对额 | 已有 |
| 方向过滤 | 全部 / 仅流入 / 仅流出 | 前端过滤 `net_in_yi` |
| 板块搜索 | 输入框，按名称包含 | 前端过滤，不改接口 |
| 成分股数 | 全部 / ≥10 / ≥30 | 有 `stocks` 则过滤；概念默认建议 ≥10 避免过碎 |

页头文案必须是完整句子，例如：`当天 · 共 31 个行业板块 · 已累计 1/1 个交易日`，禁止出现「计 个板块」这类缺数字文案。

### 2.3 底部个股（GAP11-2）

- 标题写清：`「电子」个股 · 行业 · 当天 · 31 只`
- 增加财报筛选：全部 / A / B / C / D / A+B / 无评级（对已加载列表再滤）
- 列保持 10.0.2：名称、涨跌、量比、主力买入/卖出、散户买入/卖出、主力买比、购买指数、财报、标签、区间贡献
- 取消选中或切换维度/时间后隐藏个股区

---

## 3. 财报评级完善（FR11-03）

保持「不并入购买指数」。

| 编号 | 项 | 设计 |
|------|----|------|
| FR11-03-1 | 覆盖率 | 设置页「重建财报评级」默认 `max_fetch=300`（可改 80/300/800）；启动仍 `max_fetch=0` 不阻塞 |
| FR11-03-2 | 进度 | 设置页展示：已评级 N/M、A/B/C/D 分布、本次新拉取数、阶段文案；重建中禁用重复点击 |
| FR11-03-3 | 筛选说明 | 筛选组与推荐财报组旁固定：`已评级 N/M · 未评级为 — · 筛 A 为空请重建或选无评级` |
| FR11-03-4 | 估值辅助档 | 筛选器已有 PE/PB；智能选股把 PE/PB/市值作为 **估值维度**（与财报评级并列，不互相替代） |
| FR11-03-5 | 个股页 | 财务分析卡顶部展示当前落库评级徽章 + 一句话摘要；无数据给「去设置页重建」提示 |

未评级永远显示 `—`，禁止用 PE 低冒充 A。

---

## 4. 智能选股（FR11-01）— 新一级菜单

### 4.1 入口

顶栏在「个股推荐」与「AI选股」之间新增：

`… 个股推荐 | **智能选股** | AI选股 | AI分析 | 设置`

- **智能选股**：综合打分 + 多形态指标 + 可选 AI 总结（本版主角）。
- **AI选股**：保留一句话语义选股（实现上可复用同一结果表组件）；页头加链到智能选股。
- 不删除个股筛选 / 个股推荐。

### 4.2 页面结构

```
[策略模板平铺]  均衡综合 | 主力吸筹绩优 | 放量突破 | 超跌企稳 | 板块资金共振 | 自定义
[AI 控制条]    模式：关闭 | 手动 | 自动    自动间隔：10/30/60 分钟    今日 n/上限    [生成AI点评]
[条件区]       多形态指标（见 4.3）
[权重区]       各维度 0–100 滑条，合计归一
[结果区]       综合分排序表 + 本地解读 +（可选）AI 总结
```

未点「开始选股」前结果区空态：「先选模板或条件，本地计算不消耗 AI」。

### 4.3 指标形态（必须同时支持）

| 形态 | 用途 | 例子 |
|------|------|------|
| 离散档位（多选 OR） | 与现筛选器一致 | 财报 A/B；量能放量/平稳；市值小/中/大 |
| 数值区间 | 最低～最高 | 购买指数 ≥60；主力买比 ≥55%；量比 1.2～4 |
| 权重滑条 | 只影响综合分，不淘汰 | 资金 25、财报 20、购买指数 20… |
| 硬性开关 | 打开则不满足直接剔除 | 必须主力净流入>0；必须非 ST；必须有财报评级 |
| 自然语言 | 解析后写入上面四类 | 「低估值放量的银行股」 |

维度清单（全部来自本地已有表，不新造行情源）：

| 组 | 字段 | 来源 |
|----|------|------|
| 行情 | 涨跌、5/20 日涨跌、量比、换手、振幅、价格 | `stock_snapshot` |
| 资金 | 主力买入/卖出、散户买入/卖出、主力买比、主力净流入、5 日净流入 | 快照 + `attach_flow_fields` |
| 量化 | 购买指数、情绪、暗盘、企稳分、综合评分、操作提示 | `stock_metrics` + `rating` |
| 财报 | A/B/C/D/无评级 | `stock_finance_grade` |
| 估值 | PE、PB、流通市值 | 快照 |
| 板块 | 行业、概念；板块当日净流入（行业聚合） | `stock_list` / `concept_map` / 板块资金 |
| 宏观 | 近 3 日快讯命中利好/利空次数（有则加分，无则 0） | 公告/快讯已有映射，允许命中为 0 |
| 形态 | 均线多头、站上 20 日、突破、金叉、超卖、暗中吸筹 | `stock_metrics` |

### 4.4 综合分（本地，默认必算）

对通过硬性条件的股票，每维先线性映射到 0–100，再按权重加权：

```
综合分 = Σ (维度分_i × 权重_i) / Σ 启用权重
```

默认权重（可改，保存到本地方案）：

| 维度 | 默认 | 映射要点 |
|------|------|----------|
| 购买指数 | 20 | 原值 0–100 |
| 综合评分 | 15 | `quick_score` |
| 主力资金 | 15 | 净流入分位 + 买比 |
| 量能 | 10 | 量比 0.8–3 为佳，过高略扣 |
| 财报 | 15 | A=100 B=75 C=45 D=15 无评级=50（不奖不罚） |
| 企稳 | 8 | stabilize_score，空则 50 |
| 暗盘 | 7 | dark_power |
| 板块资金 | 10 | 所属行业当日净流入分位 |

**无评级不奖不罚（记 50）**，避免覆盖率低时全部被压死。硬性「必须有评级」由开关控制。

结果默认按综合分降序，最多 100 只，分页 20/50/100。  
表列：名称、财报、行业、现价、涨跌、综合分、购买指数、量比、主力买入/卖出、散户买入/卖出、主力买比、提示、一句话本地理由。

本地理由示例：`购买指数 72 · 财报 B · 主力买比 61% · 所属电子当日净流入 +12 亿`。

### 4.5 策略模板

| 模板 | 硬性条件（摘要） | 权重偏向 |
|------|------------------|----------|
| 均衡综合 | 剔 ST | 默认权重 |
| 主力吸筹绩优 | 主力净流入>0，财报 A 或 B（无评级不进） | 资金+财报 |
| 放量突破 | 量比≥1.5，涨幅>2% 或突破标记 | 量能+购买指数 |
| 超跌企稳 | 20 日跌幅≤-10，企稳分有值 | 企稳+评分 |
| 板块资金共振 | 所属行业当日净流入>0，个股主力净流入>0 | 板块+资金 |
| 自定义 | 用户当前条件 | 用户滑条 |

套用模板覆盖条件与权重，仍可再改。

### 4.6 AI 总结：手动 / 自动 / 关闭（FR11-01-4）

原则：**先本地出名单，再决定是否调用模型。名单计算永不依赖 AI。**

| 模式 | 行为 |
|------|------|
| 关闭 | 任何选股结果都不请求大模型 |
| 手动 | 仅当用户点「生成AI点评」才调用；按钮在未配置密钥时禁用并提示去 AI 分析页 |
| 自动 | 本地结果成功后，若满足频率与上限，才调用一次 |

频率与上限（写入 `smartpick_ai_policy`，设置页与本页均可改）：

| 项 | 默认 | 范围 |
|----|------|------|
| mode | `manual` | off / manual / auto |
| min_interval_sec | 600（10 分钟） | 10 / 30 / 60 分钟 |
| daily_cap | 12 | 5 / 12 / 20 |
| max_tokens 语境 | 只送 **前 15 只** 的关键字段 + 策略名 + 条件摘要 | — |

去重：对 `hash(模板 + 条件 + 前 15 代码)` 做缓存，TTL 与 `min_interval` 相同，相同名单不重复打模型。  
超限：展示「今日 AI 次数已用完，名单仍为本地结果」。  
失败：展示具体错误（复用 10.0.2 解析），名单保留。

点评任务（固定短约束）：

> 根据下列本地量化结果，用不超过 180 字指出 3 只更值得跟踪的股票及理由，并给一句风险提示。禁止收益承诺。

### 4.7 接口

| 接口 | 说明 |
|------|------|
| GET /api/smartpick/meta | 模板、维度、默认权重、AI 策略、今日已用次数、财报覆盖 |
| POST /api/smartpick/run | body：template / conditions / weights / hard_flags；返回 items + local_summary |
| GET/POST /api/smartpick/ai-policy | 读写 mode / interval / cap |
| POST /api/smartpick/ai-comment | body：run 指纹或 items 摘要；受策略与上限约束；自动模式由 run 在服务端按策略触发也可，但 **自动不得在筛选拖动时连打**（只在「开始选股」成功后） |

`run` 在 `auto` 下若被限流，返回 `ai: {skipped: true, reason: "interval"|"cap"|"off"}`。

---

## 5. 设置与任务

| 项 | 说明 |
|----|------|
| 财报重建 | 档位 80/300/800；展示分布 |
| 智能选股 AI | 模式、间隔、日上限；与智能选股页同步 |
| 大模型 | 沿用 AI 分析页配置与测试连通 |

盘后任务：财报重建保持现有；**不**把智能选股自动点评挂到定时任务（避免无人值守刷额度）。

---

## 6. 验收标准

| 编号 | 验收项 |
|------|--------|
| AC11-1 | 板块资金分析默认只看到一套零轴直方图，不再上下各一张图 |
| AC11-2 | 行业/概念、时间、排序、方向、搜索可组合；页头有「共 N 个板块」完整数字 |
| AC11-3 | 未点板块无个股；点选后个股表含财报与资金列，且可按财报再筛 |
| AC11-4 | 未评级为 —；筛 A 为空时有覆盖率说明；重建可拉多于 80 只 |
| AC11-5 | 顶栏出现「智能选股」；模板 + 档位/区间/权重/硬性开关/一句话均可出名单 |
| AC11-6 | AI=关闭时跑选股零模型请求；手动需点击；自动受间隔与日上限约束 |
| AC11-7 | 相同前 15 只在间隔内不重复打模型；失败展示原因且名单仍在 |
| AC11-8 | 购买指数公式不因财报或智能选股权重改变 |

---

## 7. 实现顺序（文档提交后）

1. BUG11-1 直方图单通道 + GAP11-1/11-2 维度与个股筛选  
2. FR11-03 财报覆盖/文案/重建档位  
3. FR11-01 智能选股本地引擎与页面  
4. FR11-01-4 AI 开关、频率、点评缓存  

---

## 8. 已知限制

- 财报全市场仍依赖新浪披露限速，覆盖率逐步爬升；无评级不奖不罚。
- 板块中长区间资金依赖日频落库，不足天数须明示，禁止用涨跌幅填洞。
- 散户买卖、主力买比为成交额与主力差额估算，非逐笔。
- 宏观命中次数在快讯未映射到个股时为 0，不影响硬性淘汰（除非用户打开对应开关）。
- 全部结论为量化参考，不构成投资建议。
